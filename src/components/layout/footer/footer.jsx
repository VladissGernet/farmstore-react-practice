import React from 'react';

import { Logo } from '/src/components/ui/logo/logo';

import { StyledFooter } from './styled';

const Footer = () => {
  return (
    <StyledFooter>
      <Logo />
    </StyledFooter>
  )
};

export { Footer };
